interface:
  display_name: "Build Tasks"
  short_description: "Implement and review ready Beads tasks."
  default_prompt: "Use $build-tasks to implement and review the next ready Beads task."
policy:
  allow_implicit_invocation: false
