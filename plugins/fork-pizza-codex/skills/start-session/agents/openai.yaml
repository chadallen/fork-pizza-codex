interface:
  display_name: "Start Session"
  short_description: "Orient from Git and Beads, then propose work."
  default_prompt: "Use $start-session to orient this coding session and propose the next work."
policy:
  allow_implicit_invocation: false
