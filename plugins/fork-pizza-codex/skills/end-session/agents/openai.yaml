interface:
  display_name: "End Session"
  short_description: "Verify, record, commit, sync, and push."
  default_prompt: "Use $end-session to verify and close this coding session."
policy:
  allow_implicit_invocation: false
